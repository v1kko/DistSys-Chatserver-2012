#include <server.h>

using namespace std;
 
	// Must be format (username password);
void Server::addManager(string){}

void Server::setPort(int){}

	//Main control loop
	//Returns when the server should exit
void Server::start(void){}
	
void Server::stop(void){}
	

